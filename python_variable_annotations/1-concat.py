#!/usr/bin/env python3

"""Script that takes two strings
and returns its concatenation
"""


def concat(str1: str, str2: str) -> str:
    """Function that sums two floats

    Args:
        a: string one
        b: string two

    Returns:
        concatenated string
    """
    return str1 + str2
