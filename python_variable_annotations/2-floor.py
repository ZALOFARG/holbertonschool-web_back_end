#!usr/bin/env python3
"""Script that takes a float
and returns its floor
"""
import math


def floor(n: float) -> int:
    """Function that sums two floats

    Args:
        n: number

    Returns:
        number floored
    """
    return math.floor(n)
